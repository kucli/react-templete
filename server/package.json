{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tslint": "tslint -p . --type-check",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd src && nodemon --exec babel-node --presets env index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "multiparty": "^4.2.3",
    "spark-md5": "^3.0.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-register": "^6.26.0",
    "body-parser": "^1.20.0",
    "cors": "^2.8.5"
  }
}
